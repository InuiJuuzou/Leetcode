#include <iostream>
#include <vector>

using std::vector;

class Solution {
public:
    vector<vector<int>> floodFill(vector<vector<int>>& image, int sr, int sc, int color) {
        // выполнить заливку изображения, начиная с пикселя image[sr][sc]
        int m = image.size();
        int n = image[0].size();
        
         
    }
private:


};